<%- include('./partials/header') %> 
<%- include('./partials/nav') %> 
<div class="container">


    <% docs.reverse().forEach(tweet => { %> 
        <div class="media tweet">
            <div class="media-body">
            <h5 class="mt-0"><%= tweet.user %></h5>
            <%= tweet.text %> 
            </div>
        </div>
    <% }); %> 

</div>

<%- include('./partials/footer') %> 